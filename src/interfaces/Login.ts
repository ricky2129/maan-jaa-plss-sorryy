export interface LoginFormFieldType {
  username?: string;
  password?: string;
}

export interface SignupFormFieldType {
  first_name: string;
  last_name: string;
  email: string;
  password: string;
  confirmPassword: string;
}
